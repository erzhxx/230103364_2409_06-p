# Parallel Computing Labs 1-3 (Python)

pip install -r requirements.txt

    python lab1/lab1_tasks.py     # Tasks 1.1-1.3
    python lab2/lab2_tasks.py     # Tasks 2.1-2.4   (--quick for a fast check)
    python lab3/lab3_tasks.py     # Tasks 3.1-3.4   (--quick for a fast check)

Each script writes CSV files and plots to its own ./results folder.
Optional: export NUMBA_NUM_THREADS=N and OMP_NUM_THREADS=N.
Record CPU model, cores, cache sizes and OS versions for Section I of the report.
